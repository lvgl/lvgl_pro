<!--
Example
<lv_buttonmatrix map="'1' '2' '\n' '3'" ctrl_map="checked|width_3 none disabled"/>
-->

<widget>
    <api>
        <enumdef name="lv_buttonmatrix_ctrl" multi="true" help="Control flags you can apply to individual buttons">
            <enum name="none" value="0"      help="Make the button use the default state with no special control"/>
            <enum name="width_1" value="1"   help="Make the button take 1 unit of relative width in its row"/>
            <enum name="width_2" value="2"   help="Make the button take 2 units of relative width in its row"/>
            <enum name="width_3" value="3"   help="Make the button take 3 units of relative width in its row"/>
            <enum name="width_4" value="4"   help="Make the button take 4 units of relative width in its row"/>
            <enum name="width_5" value="5"   help="Make the button take 5 units of relative width in its row"/>
            <enum name="width_6" value="6"   help="Make the button take 6 units of relative width in its row"/>
            <enum name="width_7" value="7"   help="Make the button take 7 units of relative width in its row"/>
            <enum name="width_8" value="8"   help="Make the button take 8 units of relative width in its row"/>
            <enum name="width_9" value="9"   help="Make the button take 9 units of relative width in its row"/>
            <enum name="width_10" value="10"  help="Make the button take 10 units of relative width in its row"/>
            <enum name="width_11" value="11"  help="Make the button take 11 units of relative width in its row"/>
            <enum name="width_12" value="12"  help="Make the button take 12 units of relative width in its row"/>
            <enum name="width_13" value="13"  help="Make the button take 13 units of relative width in its row"/>
            <enum name="width_14" value="14"  help="Make the button take 14 units of relative width in its row"/>
            <enum name="width_15" value="15"  help="Make the button take 15 units of relative width in its row"/>
            <enum name="hidden" value="16"      help="Make the button hidden"/>
            <enum name="no_repeat" value="32"   help="Disable long-press repeat on the button"/>
            <enum name="disabled" value="64"    help="Make the button disabled (not clickable)"/>
            <enum name="checkable" value="128"   help="Make the button toggleable when clicked"/>
            <enum name="checked" value="256"     help="Keep the button in the toggled (checked) state"/>
            <enum name="click_trig" value="512"  help="Trigger LV_EVENT_VALUE_CHANGED on CLICK instead of PRESS"/>
            <enum name="popover" value="1024"     help="Show a popover when pressing the button"/>
            <enum name="recolor" value="2048"     help="Enable text recoloring with inline #color codes"/>
            <enum name="reserved_1" value="4096"  help="Reserved flag for future use"/>
            <enum name="reserved_2" value="8192"  help="Reserved flag for future use"/>
            <enum name="custom_1" value="16384"    help="Custom flag free to use by the user"/>
            <enum name="custom_2" value="32768"    help="Custom flag free to use by the user"/>
        </enumdef>

        <prop name="map" type="string[NULL]" help="Set the layout map of the button matrix using text strings and `\n` for line breaks (e.g. 'a' 'b' 'c')"/>
        <prop name="ctrl_map" type="enum:lv_buttonmatrix_ctrl[]+" help="Set the control flags for each button in the map"/>
        <prop name="one_checked" type="bool" help="Force exactly one button to stay checked at all times"/>
        <prop name="selected_button" type="int" help="Select a button by index (new lines do not count)"/>

        <parts>
            <part name="main" help="The background behind the buttons: background properties. `pad_row` and `pad_column` set the gaps between buttons."/>
            <part name="items" help="The buttons: text and background properties (transforms are not applied). Combine with the 'checked', 'pressed', 'focused' and 'disabled' states, e.g. items|checked."/>
        </parts>
    </api>
</widget>
